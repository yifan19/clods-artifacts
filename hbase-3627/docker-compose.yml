# Auto-generated by ../generate.py from ../manifest.py — do not hand-edit, edit the manifest and
# regenerate instead. Origin command this reproduces: master_hdfs_old.sh: HADOOP_NAME=hadoop-0.20.2 HBASE_NAME=hbase-0.91.0-hbase3627 ZOOKEEPER_NAME=zookeeper-3.2.2 benchmark_ycsb_hbase_old.sh add 3 1000000 3627_2 QuorumPeerMain
services:
  master:
    build: ../common
    hostname: master
    environment: &common_env
      HADOOP_NAME: "hadoop-0.20.2"
      HBASE_NAME: "hbase-0.91.0-hbase3627"
      ZOOKEEPER_NAME: "zookeeper-3.2.2"
      CASSANDRA_NAME: ""
      YCSB_NAME: "ycsb-0.1.4"
      YCSB_BINDING: "hbase"
      YCSB_TABLE: "ycsb"
      YCSB_CF: "cf"
      HIBENCH_NAME: ""
      HIBENCH_WORKLOAD: ""
      ZK_NODE_CMD: ""
      WORKLOAD: "ycsb_hbase_old"
      APP_NAME: "QuorumPeerMain"
      APP_TARGET: "slaves"
      NUM: "1000000"
      ROUNDS: "baseline 1 2 3 4"
      ENABLE_YARN: "0"
      MASTER_HOST: "master"
      SLAVE_HOSTS: "slave1 slave2 slave3"
      RESULTS_DIR: "/results"
    volumes:
      - ../binaries:/binaries:ro
      - ./plans:/plans:ro
      - ./results:/results
    networks: [bugnet]
    command: ["/opt/entrypoint.sh"]

  slave1:
    build: ../common
    hostname: slave1
    environment:
      <<: *common_env
      ZK_MYID: "1"
    volumes:
      - ../binaries:/binaries:ro
      - ./plans:/plans:ro
    networks: [bugnet]
    command: ["/opt/entrypoint.sh"]

  slave2:
    build: ../common
    hostname: slave2
    environment:
      <<: *common_env
      ZK_MYID: "2"
    volumes:
      - ../binaries:/binaries:ro
      - ./plans:/plans:ro
    networks: [bugnet]
    command: ["/opt/entrypoint.sh"]

  slave3:
    build: ../common
    hostname: slave3
    environment:
      <<: *common_env
      ZK_MYID: "3"
    volumes:
      - ../binaries:/binaries:ro
      - ./plans:/plans:ro
    networks: [bugnet]
    command: ["/opt/entrypoint.sh"]


networks:
  bugnet: {}
