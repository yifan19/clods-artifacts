# Auto-generated by ../generate.py from ../manifest.py — do not hand-edit, edit the manifest and
# regenerate instead. Origin command this reproduces: master_zookeeper.sh: HADOOP_NAME=hadoop-2.8.2 ZOOKEEPER_NAME=zookeeper-3.5.0-SNAPSHOT benchmark_ycsb_zk.sh {add|baseline} 1 1000000 1851_2 QuorumPeerMain (commented out in the checked-in script; command shape reused from the analogous 1434/1900 blocks in the same file)
services:
  master:
    build: ../common
    hostname: master
    environment: &common_env
      HADOOP_NAME: "hadoop-2.8.2"
      HBASE_NAME: "hbase-1.2.5"
      ZOOKEEPER_NAME: "zookeeper-3.5.0-SNAPSHOT"
      CASSANDRA_NAME: ""
      YCSB_NAME: "ycsb-0.18.0-SNAPSHOT"
      YCSB_BINDING: ""
      YCSB_TABLE: "ycsb"
      YCSB_CF: "cf"
      HIBENCH_NAME: ""
      HIBENCH_WORKLOAD: ""
      ZK_NODE_CMD: "create /benchmark"
      WORKLOAD: "ycsb_zk"
      APP_NAME: "QuorumPeerMain"
      APP_TARGET: "slaves"
      NUM: "1000000"
      ROUNDS: "baseline 1 2 3 4 5 6"
      ENABLE_YARN: "0"
      MASTER_HOST: "master"
      SLAVE_HOSTS: "slave1 slave2 slave3"
      RESULTS_DIR: "/results"
    volumes:
      - ../binaries:/binaries:ro
      - ./plans:/plans:ro
      - ./results:/results
    networks: [bugnet]
    command: ["/opt/entrypoint.sh"]

  slave1:
    build: ../common
    hostname: slave1
    environment:
      <<: *common_env
      ZK_MYID: "1"
    volumes:
      - ../binaries:/binaries:ro
      - ./plans:/plans:ro
    networks: [bugnet]
    command: ["/opt/entrypoint.sh"]

  slave2:
    build: ../common
    hostname: slave2
    environment:
      <<: *common_env
      ZK_MYID: "2"
    volumes:
      - ../binaries:/binaries:ro
      - ./plans:/plans:ro
    networks: [bugnet]
    command: ["/opt/entrypoint.sh"]

  slave3:
    build: ../common
    hostname: slave3
    environment:
      <<: *common_env
      ZK_MYID: "3"
    volumes:
      - ../binaries:/binaries:ro
      - ./plans:/plans:ro
    networks: [bugnet]
    command: ["/opt/entrypoint.sh"]


networks:
  bugnet: {}
