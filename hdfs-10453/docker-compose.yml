# Auto-generated by ../generate.py from ../manifest.py — do not hand-edit, edit the manifest and
# regenerate instead. Origin command this reproduces: master_dfsio.sh (as 'instrumentation_bug1'): HADOOP_NAME=hadoop-2.5.2 benchmark_ycsb_hbase.sh {add|baseline} 1 1000000 kairux_y Namenode
services:
  master:
    build: ../common
    hostname: master
    environment: &common_env
      HADOOP_NAME: "hadoop-2.5.2"
      HBASE_NAME: "hbase-1.2.5"
      ZOOKEEPER_NAME: ""
      CASSANDRA_NAME: ""
      YCSB_NAME: "ycsb-0.12.0"
      YCSB_BINDING: "hbase10"
      YCSB_TABLE: "ycsb"
      YCSB_CF: "cf"
      HIBENCH_NAME: ""
      HIBENCH_WORKLOAD: ""
      ZK_NODE_CMD: ""
      WORKLOAD: "ycsb_hbase"
      APP_NAME: "Namenode"
      APP_TARGET: "master"
      NUM: "1000000"
      ROUNDS: "1 2 3 4"
      ENABLE_YARN: "0"
      MASTER_HOST: "master"
      SLAVE_HOSTS: "slave1 slave2 slave3"
      RESULTS_DIR: "/results"
    volumes:
      - ../binaries:/binaries:ro
      - ./plans:/plans:ro
      - ./results:/results
    networks: [bugnet]
    command: ["/opt/entrypoint.sh"]

  slave1:
    build: ../common
    hostname: slave1
    environment:
      <<: *common_env
      ZK_MYID: "1"
    volumes:
      - ../binaries:/binaries:ro
      - ./plans:/plans:ro
    networks: [bugnet]
    command: ["/opt/entrypoint.sh"]

  slave2:
    build: ../common
    hostname: slave2
    environment:
      <<: *common_env
      ZK_MYID: "2"
    volumes:
      - ../binaries:/binaries:ro
      - ./plans:/plans:ro
    networks: [bugnet]
    command: ["/opt/entrypoint.sh"]

  slave3:
    build: ../common
    hostname: slave3
    environment:
      <<: *common_env
      ZK_MYID: "3"
    volumes:
      - ../binaries:/binaries:ro
      - ./plans:/plans:ro
    networks: [bugnet]
    command: ["/opt/entrypoint.sh"]


networks:
  bugnet: {}
