# Auto-generated by ../generate.py from ../manifest.py — do not hand-edit, edit the manifest and
# regenerate instead. Origin command this reproduces: master_yarn.sh: HADOOP_NAME=hadoop-3.0.0-SNAPSHOT benchmark_yarn.sh {add|baseline} 2..4 20 5  (HiBench2, workloads micro.terasort + websearch.pagerank toggled via conf/benchmarks.lst — see ~/artifacts/hibench.lst, a snapshot of that exact file mid-run)
services:
  master:
    build: ../common
    hostname: master
    environment: &common_env
      HADOOP_NAME: "hadoop-3.0.0-SNAPSHOT"
      HBASE_NAME: ""
      ZOOKEEPER_NAME: ""
      CASSANDRA_NAME: ""
      YCSB_NAME: ""
      YCSB_BINDING: ""
      YCSB_TABLE: "ycsb"
      YCSB_CF: "cf"
      HIBENCH_NAME: "HiBench2"
      HIBENCH_WORKLOAD: "websearch.pagerank"
      ZK_NODE_CMD: ""
      WORKLOAD: "hibench"
      APP_NAME: "Resour"
      APP_TARGET: "master"
      NUM: "5"
      ROUNDS: "baseline 2 3 4"
      ENABLE_YARN: "1"
      MASTER_HOST: "master"
      SLAVE_HOSTS: "slave1 slave2 slave3"
      RESULTS_DIR: "/results"
    volumes:
      - ../binaries:/binaries:ro
      - ./plans:/plans:ro
      - ./results:/results
    networks: [bugnet]
    command: ["/opt/entrypoint.sh"]

  slave1:
    build: ../common
    hostname: slave1
    environment:
      <<: *common_env
      ZK_MYID: "1"
    volumes:
      - ../binaries:/binaries:ro
      - ./plans:/plans:ro
    networks: [bugnet]
    command: ["/opt/entrypoint.sh"]

  slave2:
    build: ../common
    hostname: slave2
    environment:
      <<: *common_env
      ZK_MYID: "2"
    volumes:
      - ../binaries:/binaries:ro
      - ./plans:/plans:ro
    networks: [bugnet]
    command: ["/opt/entrypoint.sh"]

  slave3:
    build: ../common
    hostname: slave3
    environment:
      <<: *common_env
      ZK_MYID: "3"
    volumes:
      - ../binaries:/binaries:ro
      - ./plans:/plans:ro
    networks: [bugnet]
    command: ["/opt/entrypoint.sh"]


networks:
  bugnet: {}
